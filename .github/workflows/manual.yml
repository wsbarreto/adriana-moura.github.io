name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI or API.
on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Deploy to production
      if: github.ref == 'refs/heads/main'
      run: |
        echo "Deploying to production server..."
        # Comandos para fazer o deploy
        # Você pode adicionar scripts ou comandos específicos para o seu processo de deploy

    - name: Notify team
      if: failure()
      run: |
        echo "Notifying team about the failure..."
        # Comandos para notificação, como enviar um e-mail ou mensagem no Slack
